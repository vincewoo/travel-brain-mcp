-- Optional seed example.
-- Replace the UUID below with an existing auth.users.id before running.
-- The MCP server will create the profile automatically, so this file is not required.

-- insert into public.profiles (id, display_name)
-- values ('00000000-0000-0000-0000-000000000000', 'Traveler')
-- on conflict (id) do update set display_name = excluded.display_name;
